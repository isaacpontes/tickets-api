const path = require('path');

module.exports = {
  "config": path.join(__dirname, "config", "sequelize-cli.js"),
  "migrations-path": path.join(__dirname, "src", "sequelize", "migrations"),
  "seeders-path": path.join(__dirname, "src", "sequelize", "seeders")
}